package pe.ecolecta.presentation.calidad

import androidx.compose.ui.graphics.Color

/**
 * Acento violeta exclusivo del rol Técnico de calidad (el mismo `--eh-violet` del panel web):
 * distingue de un vistazo en qué parte de la app está el usuario. No forma parte de
 * [pe.ecolecta.presentation.design.Colores] a propósito — es de un solo rol, no un color semántico compartido.
 */
val CalidadMorado = Color(0xFF655497)
