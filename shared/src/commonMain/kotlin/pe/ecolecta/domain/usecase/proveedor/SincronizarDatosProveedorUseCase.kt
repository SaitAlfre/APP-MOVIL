package pe.ecolecta.domain.usecase.proveedor

/** No hay backend todavía (Fase 4): confirma que la información mostrada es la última disponible localmente. */
class SincronizarDatosProveedorUseCase {
    operator fun invoke(): String =
        "La sincronización con el servidor llega en la Fase 4. Esta es la información más reciente guardada en el dispositivo."
}
